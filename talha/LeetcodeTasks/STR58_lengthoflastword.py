class Solution(object):
    def lengthOfLastWord(self, s):
        lstr=s.split(" ")
        l=len(lstr)
        return (len(lstr[l-1]))class Solution(object):
    def lengthOfLastWord(self, s):
        lstr=s.split(" ")
        l=len(lstr)
        return (len(lstr[l-1]))